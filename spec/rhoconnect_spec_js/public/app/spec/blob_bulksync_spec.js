var sync_server_url = "http://"+SYNC_SERVER_HOST+":"+SYNC_SERVER_PORT;
//var Product = ORM.AddModel('Product'); 

describe("Rhoconnect Client module Test Starts Here", function() {


	beforeEach(function() {
					
		});
		
	afterEach(function() {
			
    });
			
		//Blob bulk sync test
		
		it("VT295-104 | Blob bulk sync functionality test  | All Blob files should get download from sync server successfully", function() {  
			 
			
	
		});
		
		it("VT295-105 | Blob bulk sync test when local db has some blob files  | All current blobs in DB-files folder should get replaced with current Blob images downloaded from sync server", function() {  
			 
			
	
		});
		
		it("VT295-106 | Check bulk_status parameter in sync notification call back during blob bulk sync  | All state(start,download,change_db,blob,ok,and complete)should be reported inside notification callback", function() {  
			 
			
	
		});
		
		it("VT295-107 |   | All state(start,download,change_db,blob,ok,and complete)should be reported inside notification callback", function() {  
			 
			
	
		});
		
});
		